CREATE TABLE airline_delays (
    year INT,
    month INT,
    carrier_code VARCHAR(10),
    carrier_name VARCHAR(100),
    airport_code VARCHAR(10),
    airport_name VARCHAR(150),
    total_flights FLOAT,
    delayed_flights_15min FLOAT,
    carrier_delay_count FLOAT,
    weather_delay_count FLOAT,
    nas_delay_count FLOAT,
    security_delay_count FLOAT,
    late_aircraft_delay_count FLOAT,
    cancelled_flights FLOAT,
    diverted_flights FLOAT,
    total_delay_minutes FLOAT,
    carrier_delay_minutes FLOAT,
    weather_delay_minutes FLOAT,
    nas_delay_minutes FLOAT,
    security_delay_minutes FLOAT,
    late_aircraft_delay_minutes FLOAT,
    date DATE,
    delay_rate FLOAT,
    avg_delay_per_delayed_flight FLOAT,
    season VARCHAR(20),
    carrier_delay_pct FLOAT,
    weather_delay_pct FLOAT,
    nas_delay_pct FLOAT,
    security_delay_pct FLOAT,
    late_aircraft_delay_pct FLOAT
);
